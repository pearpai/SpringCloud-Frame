

eureka:
  client:
    service-url:
      defaultZone: http://wuyunfeng:wuyunfeng@localhost:8761/eureka/

server:
  port: 8083
spring:
  application:
    name: feign-service

feign:
  hystrix:
    enabled: true
